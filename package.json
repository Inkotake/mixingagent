{
  "name": "mixingagent",
  "private": true,
  "version": "0.1.0",
  "description": "AI Sound Engineer (调音师) — Intelligent mixing assistant built on wing-mcp",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18"
  }
}
